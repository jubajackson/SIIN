@{
    ViewBag.Title = "Second II None MC Events";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@if (Model.Count > 0)
{
    <div class="gridContainer">
        <div>
            &nbsp;</div>
        @for (var rowCount = 0; rowCount < Model.Count; rowCount++)
        {
            <div class="row">
                @for (var i = 0; i < 3; i++)
                {
                    if (rowCount < (Model.Count))
                    {
              
                        <div class="large-1 photoGalleryColumns">
                            <img alt="Profile Picture" src="https://graph.facebook.com/@Model[rowCount].FacebookId/picture?type=small" />
                        </div>
                        <div class="large-3 photoGalleryColumns" style="border-right: 2px solid #D9D9D9;">
                            <a target="_blank" href="https://www.facebook.com/profile.php?id=@Model[rowCount].FacebookId&sk=events">
                                @Model[rowCount].Name
                            </a>
                        </div>
                        rowCount++;

                    }
                    else
                    {
                        <div class="large-1 photoGalleryColumns">
                        </div>
                        <div class="large-3 photoGalleryColumns">
                        </div>
                    }
                }
            </div>
                rowCount--;
        }
        <div>
            &nbsp;</div>
    </div>
}